<?php 
    // Pertemuan 2 - PHP Dasar
    // Sintaks PHP

    // standar Output
    // echo, print (mencetak di layar)
    // print_r (mencetak isi array)
    // var_dump (melihat isi variabel)
    // echo "Felix Edna Santosa";

    // penulisan sintaks php
    // 1. php di dalam html
    // 2. html di dalam php

    // variabel dan tipe data
    // variabel
    // nama tidak boleh diawali dengan angka tapi boleh mengandung angka
    // $nama = "Felix"; // tidak mendefinisikan tipe data nya apa
    // echo "Halo, nama saya $nama"; // kalau pakai kutip satu akan terjadi interpolasi

    // operator
    // aritmatika
    // +, -, *, /, %
    // $x = 20;
    // $y = 10;

    // echo $x + $y;
    // echo $x - $y;
    // echo $x * $y;
    // echo $x / $y;
    // echo $x % $y;

    // operator string / concatenation
    // .
    // $nama_depan = "Felix";
    // $nama_belakang = "Edna Santosa";
    // echo $nama_depan . " " . $nama_belakang;

    // operator assignment
    // operator penugasan
    // =, +=, -=, *=, /=, %=, .=
    // $x = 1;
    // $x += 5;
    // echo $x;
    // $nama = "Felix";
    // $nama .= " ";
    // $nama .= "Santosa";
    // echo $nama;

    // operator perbandingan
    // >, <, >=, <=, ==, !=
    // var_dump(1 < "5"); // operator perbandingan tidak mengecek tipe data hanya angka

    // operator identitas
    // ===, !==
    // var_dump(1 === "1");

    // operator logika
    // &&, ||, !
    // $x = 10;
    // var_dump($x < 20 && $x > 20);
?>